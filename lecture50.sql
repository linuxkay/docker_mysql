# get data id, last_name, gender male and limit showing 10
select id, last_name, gender from users where gender = 1 limit 10;
# get data id, last_name, gender female and limit showing 10
select id, last_name, gender from users where gender =2 limit 10;
